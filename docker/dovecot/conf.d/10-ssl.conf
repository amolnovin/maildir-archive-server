# ---- SSL/TLS (نحو Dovecot 2.4) ----
# گواهی در entrypoint ساخته یا از پوشه certs/ کپی می‌شود
ssl = yes
ssl_server_cert_file = /tmp/certs/mail.crt
ssl_server_key_file = /tmp/certs/mail.key
ssl_min_protocol = TLSv1.2
